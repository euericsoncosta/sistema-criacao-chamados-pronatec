<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h2 class="fw-bold mb-0">
      <i class="fas fa-building me-2 text-primary"></i>Gestão de Setores
    </h2>
    <p class="text-muted small mb-0">
      Administre os departamentos e áreas de suporte do sistema.
    </p>
  </div>
  <a href="/setores/novo" class="btn btn-primary px-4 shadow-sm">
    <i class="fas fa-plus-circle me-2"></i>Novo Setor
  </a>
</div>

<div class="card shadow-sm border-0">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-light">
          <tr>
            <th class="ps-4" style="width: 100px;">ID</th>
            <th>Nome do Setor / Departamento</th>
            <th class="text-center" style="width: 200px;">Data de Registo</th>
            <th class="text-end pe-4" style="width: 150px;">Ações</th>
          </tr>
        </thead>
        <tbody>
          {{#each setores}}
            <tr>
              <td class="ps-4">
                <span
                  class="badge bg-light text-muted border"
                >#{{this.id}}</span>
              </td>
              <td>
                <div class="d-flex align-items-center">
                  <div
                    class="bg-primary bg-opacity-10 text-primary rounded-circle me-3 d-flex align-items-center justify-content-center"
                    style="width: 40px; height: 40px;"
                  >
                    <i class="fas fa-tag"></i>
                  </div>
                  <span class="fw-bold text-dark">{{this.nome}}</span>
                </div>
              </td>
              <td class="text-center text-muted small">
                {{#if this.created_at}}
                  {{formatDate this.created_at}}
                {{else}}
                  --/--/----
                {{/if}}
              </td>
              <td class="text-end pe-4">
                <div class="btn-group shadow-sm">
                  <a
                    href="/setores/editar/{{this.id}}"
                    class="btn btn-sm btn-white border border-end-0"
                    title="Editar"
                  >
                    <i class="fas fa-edit text-warning"></i>
                  </a>
                  <button
                    class="btn btn-sm btn-white border text-danger"
                    title="Apagar"
                    disabled
                  >
                    <i class="fas fa-trash-alt"></i>
                  </button>
                </div>
              </td>
            </tr>
          {{else}}
            <tr>
              <td colspan="4" class="text-center py-5">
                <div class="py-4">
                  <i class="fas fa-folder-open fa-3x text-light mb-3"></i>
                  <h5 class="text-muted">Nenhum setor encontrado</h5>
                  <p class="text-muted small">Clique no botão "Novo Setor" para
                    começar.</p>
                  <a href="/setores/novo" class="btn btn-sm btn-primary mt-2">
                    Adicionar Primeiro Setor
                  </a>
                </div>
              </td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

  <div class="card-footer bg-white border-top-0 py-3">
    <div class="row align-items-center">
      <div class="col">
        <p class="text-muted small mb-0">
          A mostrar
          <strong>{{setores.length}}</strong>
          setores registados.
        </p>
      </div>
      <div class="col text-end">
        <span class="text-muted small">
          <i class="fas fa-info-circle me-1"></i>
          Apenas administradores podem gerir setores.
        </span>
      </div>
    </div>
  </div>
</div>